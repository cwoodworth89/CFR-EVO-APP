# SEED DATA ONLY -- not read at runtime.
# public.vocabulary is the single source of truth; the parser and the Whisper bias
# prompt read from the database. This file is consumed once by
# backend/scripts/import_gis_data.py (step7) to populate that table on a fresh
# install, and is NOT kept in sync afterwards. To add a term, insert it into
# public.vocabulary -- editing this file has no effect on a running system.
routine
emergency