# Post-freeze backlog

Anything discovered **during the freeze** that is not already a punch list item goes here
as **one line**, and is not investigated until the freeze lifts.

This file exists to give the freeze an exit condition. The punch list grew to 68 items
because every hardening pass was also a discovery pass, and discovery has no natural end:
each item found under CLAUDE.md §7 ("find the source") legitimately produces two or three
more. That is correct behaviour for the system and the wrong behaviour for a freeze.

**The rule**: if it is not already in [`debug_and_qa_punchlist.md`](debug_and_qa_punchlist.md),
it does not get worked, characterized, or root-caused now. Write the line. Move on.

**The exception**: a 🔴 crew-visible defect — one that produces plausible wrong operational
output crews cannot detect — is promoted into the punch list immediately. That is the whole
reason the severity column exists.

| Date | Found while | One line |
|:--|:--|:--|
| 2026-08-31 | External call audit | ✅ **FIXED same day** — Leaflet marker icons loaded from `raw.githubusercontent.com` + `cdnjs.cloudflare.com`** — offline, the gold *incident* marker and the blue candidate markers do not render, silently (a failed `<img>` is not a JS error). 6 sites across `BlockParcelPanel`, `PropertySatellitePanel`, `RouteOverviewPanel`. Vendored to local SVG shared from `components/map/mapIcons.js`; verified absent from the built bundle. [`external_calls.md`](external_calls.md) §3.1. |
| 2026-08-31 | External call audit | ✅ **DONE 2026-09-09** — the import is gone and the built bundle no longer names either host (checked in `dist/assets/*.js`). `RoutingOverlay.jsx` imported `leaflet-routing-machine` but never called `L.Routing.control` — it draws routes from our own OSRM via `apiClient`. The unused import still pulls the library's default `router.project-osrm.org` and `api.mapbox.com` URLs into the shipped bundle. **No request is made** (verified: no `serviceUrl`, no router instantiation), so this is dead weight, not a live external call. Drop the import. |
| 2026-08-31 | External call audit | Road closure daemon (`server.py:129`) hits `open511.gov.bc.ca` hourly and fails silently; the kiosk cannot tell a crew the closure list is stale. §6.1 question, not a coding fix. [`external_calls.md`](external_calls.md) §2.1. |
| 2026-08-31 | External call audit | `backend/.env` still exports `GOOGLE_APPLICATION_CREDENTIALS` for an orphaned GCP service-account key (live `private_key`, project `cfr-dispatch-mapping`). No code reads it; never committed; gitignored. Revocation is a Google console action. [`external_calls.md`](external_calls.md) §7. |
| 2026-08-31 | Operator reported review-panel audio not playing (cause was the operator's earbuds, not the app) | `VerificationSidebar.jsx:219` falls back to the raw `selectedCall.audio_url` when `audioSignedUrl` has not resolved yet: `src={audioSignedUrl || selectedCall.audio_url}`. That value is always server-relative (`/api/audio/<id>.wav`), so it resolves against the page origin — port 80, nginx — whose `try_files ... /index.html` answers **867 bytes of `text/html` at status 200**. Confirmed in `/var/log/nginx/access.log`: the operator's Firefox fetched it and got HTML. Currently harmless — the effect then sets the absolute `:8000` URL, the `key` changes, React remounts the element and playback works — so it is a wasted request, not an outage. But it is the exact relative-path trap CLAUDE.md §1 forbids, and if the correction ever did not fire the player would fail silently: the `<audio>` has no `onError`, so a decode failure shows nothing at all. Drop the fallback and render the player only once `audioSignedUrl` is set; add an `onError`. |
| 2026-08-31 | Reviewing the Whisper training pipeline | `extract_training_data.py` fetches `/api/dispatches?limit=500` with no pagination, and the endpoint returns **newest-first**. `public.dispatches` now holds **530** rows, so the 30 oldest verified calls are already invisible to the extractor and the blind spot grows with every call. The endpoint's cap is `le=5000`; the fix is a page loop (or `limit=5000`), plus filtering server-side on `feedback_submitted` rather than pulling 500 rows and discarding most. Not crew-visible — it silently shrinks the training set, it does not produce wrong dispatch output. |
| 2026-08-31 | Checking which Whisper model the kiosk runs | **The fine-tuned model has never been in service.** `backend/.env` sets `WHISPER_MODEL=base`, and all **37** `cfr-agent` starts in the journal — which reaches back to 2026-06-19, a month before the Jul 17 training run — loaded `'base'`. No `whisper-base-cfr-ct2/` exists anywhere on the kiosk; what survives is a headless `backend/models/model.bin` (76 MB, Jul 17, confirmed a CTranslate2 `WhisperSpec` blob from its header) with no `config.json`/`vocabulary.json`/tokenizer beside it, so that directory is unloadable by faster-whisper. [`milestones.md:54`](milestones.md) ("WER 22.6% → 3.5%") and [`database_backup_runbook.md:40`](briefings/database_backup_runbook.md) (lists the ct2 dir as a backup target) both read as though it shipped; correct both. Recoverable, not lost: 544 source `.wav`s are intact in `backend/audio_files/recordings` and 468 verified calls are opted in, so this is a re-run, not a rebuild. |
| 2026-09-01 | Backing the fine-tuned model off the kiosk | `pull_backups.ps1` oscillates: it keeps `-KeepFull 4` locally while the kiosk retains 7, so every run re-downloads the 3 full dumps it rotated away on the previous run — ~36 MB of pointless transfer per invocation, forever. Observed across two consecutive runs 2026-09-01. Fix is to pull only the newest `KeepFull` full dumps rather than pulling all and deleting after. Harmless to the archives themselves; the critical dumps and model archives are unaffected. |
| 2026-09-02 | Looking for the operator's `[PA]` tags | **`review_notes` exists twice and the column is the wrong one.** `target->>'review_notes'` holds 276 notes; the `review_notes` column holds 60, a partial mirror that never disagrees where both exist (3 column-only rows). Every `[PA]` tag (22) is in the JSON copy, so a query on the column reports zero — which is exactly what happened in the 2026-09-02 STT review. The 2026-08-31 `promote_verified_fields_to_columns` migration did not finish the job for notes, and the review panel presumably still writes to `target`. Either backfill the column and point the UI at it, or drop the column; two stores for one field is the "stored beside the thing it was derived from" trap. Read notes as `COALESCE(target->>'review_notes', review_notes)` until then. |
| 2026-09-02 | Fixing the parser folding business names into the street | **Five hand-typed street-suffix lists in the parser, none of them the vocabulary.** `location.py` had four (`normalize_street_suffix`, `clean_location_text`, `extract_subaddress_info`, `split_street_base_suffix`) plus `_SUFFIX_EQUIV`, and `announcement.py:38` has a sixth with no abbreviations at all (`street|avenue|...|lane` -- no `st`, no `crt`). They disagreed: three lacked `crt`, which is the form the operator writes and the fine-tuned model now emits. `_SUFFIX_EQUIV` itself says the canonical forms come from `public.roads.roadtype` via the `street_suffix` vocabulary (38 terms). The 2026-09-02 fix collapsed the four in `location.py` onto `_SUFFIX_EQUIV`; the remaining step is to build that map from the vocabulary at load time and use it in `announcement.py:38` too. Not crew-visible on its own; the crew-visible defects it caused are fixed. |
| 2026-09-03 | Staleness audit | **`backend/api/init_db.sql` has diverged from the live schema and nothing replays `backend/migrations/`.** Compose mounts `init_db.sql` into `docker-entrypoint-initdb.d`; no runner applies the 25 migrations. A second hall built from `docker compose up` starts on the 2026-08-20 schema (`parcels.lat`, `intersections.zone_id` still present). Deployment design is deferred by the operator; this is the first thing it must solve. |
| 2026-09-03 | Staleness audit | `tools/audit_staleness.py` (new) overlaps `audit_skill_references.py --docs` on dangling paths. **Resolved 2026-09-05**: the dangling-path section was removed from `audit_staleness.py`; the pre-commit hook's checker owns it. Two tools that check the same thing will drift; fold the extra checks (env vars, compose names, `file://` links, punch-list status, migration-vs-code) into the older script and delete the new one. |
| 2026-09-03 | Staleness audit | `backend/scripts/download_gis_data.py` and `update_gis_data.py` both download the City shapefile zips; `update_gis_data.py` adds a 6-day throttle and winding-order fix. One of them is redundant. Not touched during the freeze. |
| 2026-09-04 | Operator wants production and development code visibly separated | **Move development-only scripts out of `backend/scripts/` into a top-level tree** (proposed `tools/`): the *QA harnesses*, *STT / MLOps*, *Ad-hoc inspection* sections, `oneshot/`, `audit_*.py`, and root `scripts/`. Blockers to solve first: `backup_db.sh` is pinned in the kiosk crontab by absolute path; most scripts locate the package via `sys.path.insert` relative to their own file; fourteen skills and the scripts README cite paths (`audit_skill_references.py --scripts` checks one directory). **Done 2026-09-04**: `tools/` created, 32 scripts moved, `tools/_repo.py` replaces the per-script path climbing, `backup_db.sh` stayed where the crontab pins it, the hook checks both READMEs. |
| 2026-09-05 | Harness tails (`briefings/harness_tails_2026-09-05.md`) | **Named places are not resolvable**: `Eagle Mountain Park`, `Port Mann Bridge`, a Lougheed on-ramp. The operator's own review note asks for it ("gives a park; we need to be able to send"). `custom_places` was dropped (#7) because its coordinates were hand-entered; the right shape is a table fed from City open data (parks, structures), resolved through PostGIS like everything else (§6.2). |
| 2026-09-05 | Harness tails | **A bare street name never reaches the ladder**: `get_coordinates("Pinetree Way")` returns before step 1 because there is no house number, so not even the labelled street centroid is offered. Whether a 5 km arterial's centroid is worth showing is the operator's call; today the kiosk shows unresolved. |
| 2026-09-05 | Harness tails | **"Thor" heard as "four" and glued onto the house number** (`3356 Thor Crt` → `33564 Crt`, twice). Hotword coverage first (#18). A guard is available and sourced: no civic number in `public.parcels` has more than 4 digits (max 6000), so a 5-digit house number is never real. |
| 2026-09-05 | Harness tails | **The model in service mangles grid numbers** on a few calls (`map grid 1010` for 109, `904` for 94) even without the prompt (#63). Distinct from the prompt echo; STT-side; 7 of 42 hard calls. |
| 2026-09-05 | Harness tails | `download_gis_data.py` and `update_gis_data.py` are **not** redundant, correcting the 2026-09-04 line above: one pulls roads, road names and the boundary as GeoJSON over REST; the other pulls the address and zone shapefile zips. Both stay. |
| 2026-09-05 | Dead-code sweep (`vulture`) | `dispatch_uploads` stays in `init_db.sql` and the live database although its ORM model (`DispatchUploadModel`, no reader or writer) was removed. Drop the table when the schema is reconciled with `init_db.sql` (line above, 2026-09-04). |
| 2026-09-05 | Dead-code sweep | `notify_it_alert` in `notification_service` has no caller since `send_it_alert_if_unhealthy` (never called itself) was removed from `health_watchdog.py`. The ntfy broker's other entry points are untouched. |
| 2026-09-05 | Parity check (#66) | CLAUDE.md §5 says address normalisation is "one implementation, both sides". It is two: `addressUtils.js` (`sanitizeAddress`) and `parser/location.py` (`extract_subaddress_info`), written separately. `test_address_sanitizer_roads.py` now runs every City road name through the JS one; the same test against the Python one is cheap and not written. |
| 2026-09-05 | `test_evaluations_router` | `GET /api/metrics/summary` (`routers/evaluations.py`) returns a **hardcoded** `telemetry` block (12.4 s alert latency, 1.82 s STT inference, 34.2 % VAD) and `containers` list ("99.9%" uptime), and `admin/SystemMetricsPanel.jsx` falls back to the same literals when the fetch fails. Invented figures on an admin panel (CLAUDE.md §6.1). Nothing measures them today; either measure or show `--`. **Removed the same day**: the endpoint returns null for every telemetry figure and for `containers`, and the panel shows `--`; the panel's own literal fallbacks and the stage bar's invented durations went with them. |
| 2026-09-05 | Telemetry removed | The agent already times every call, `dsp_ms`, `stt_ms`, `gis_ms`, `bcast_ms` in `pipeline/phase1.py` (`metrics`) and `audio_save_ms`, `dsp_ms`, `stt_ms` in `phase2.py`, and keeps them only in the worker's in-memory profile. Storing them on the dispatch row (`target->'pipeline_metrics'`) would let `/api/metrics/summary` report real medians and the admin panel's stage bar real durations. Container health needs either a probe the API can make (HTTP to tiles, ntfy, mosquitto) or the agent's `health_watchdog` audit written where the API can read it. |
| 2026-09-05 | 5-digit house numbers, measured (extends the Thor/four line above) | **Source**: `public.parcels` max civic number 6000, 4 digits, zero 5-digit rows; `public.roads` address ranges max 7351. **Corpus**: 7 of 565 dispatches parsed a 5+ digit house, all STT digit dictation reassembled by the sanitizer join ("300, zero, zero" → 30000; "33, 56, 4, court" → 33564 with *Thor* lost), only 4 raw transcripts carry a 5-digit token; 0 of 565 verified addresses have 5+ digits. **Simulated rule**, delete one digit and keep only readings that exist on that street in `parcels`: 4 unique (2003 Gable, 3000 Lougheed ×2, 1300 Pinetree), 1 ambiguous (2983/2988 Robson → the candidate selector), 3 street unknown (Thor ×2, Riverband) → unresolved as today. Pitt Meadows/Maple Ridge Lougheed numbers (19xxx-24xxx) yield no Coquitlam reading (Coquitlam Lougheed is 502-3064). **Blocked on the operator** (§7.6): does CFR get dispatched to legitimate 5-digit civic addresses (mutual aid into Pitt Meadows, Maple Ridge, Surrey), and if so how does the announcement name the city? Falsifier: one verified address with 5+ digits. **Ruled and built the same day**: out-of-city dispatches are rare and not handled; step 1b of the ladder reads the number against the parcel table, punch-list #69. |
| 2026-09-05 | #72 (closed) | **Rule A, the phase-1 location gate**: publish a location in the preliminary payload only when the resolver answered at the parcel or intersection tier, else the Tier 1 card until phase 2. Measured with `tools/harness_phase1.py` on 507 recordings: 81 locations withheld per 507 calls (15 of them exact), 48 of 52 wrong streets in the first minute removed. The operator's rule (unknown beats a guess) points at it. **Built 2026-09-05 (`1997c05`)** on the operator's go: `location_pending` in the preliminary payload, phase 2 places the call; six live tests. Holdout replay the same day: 3 of 44 locations withheld. Live after the next restart. |
| 2026-09-05 | Operator design | **Progressive dispatch**: the kiosk pops on the tones and fills in units, call type, address with subaddress and calculated grid, near roads, talk group, then the spoken grid; ntfy fires once when a driver can act and again only on a correction. Spec with the rulings and the measurement to run first: [`architecture/progressive_dispatch.md`](architecture/progressive_dispatch.md). |
| 2026-09-05 | #71 (closed) | **Hotword experiment 4, unmeasured**: 27 of the 57 live terms are template and apparatus words the model was fine-tuned on (*respond*, *Engine*, *Light Attack Vehicle*), about 45 tokens. Whether biasing them still helps is one holdout run and one corpus run with them removed; the operator's call whether to spend the kiosk time. Experiment 3 (names without suffixes) measured a wash and stays off. |
| 2026-09-05 | #34b (closed) | **Response type unparsed on overdose calls**: 11 of the 12 overdose dispatches since 2026-08-23 store no `response_type` (they render UNKNOWN, amber, by #31's design). The announcement says *respond emergency*; find why the parser drops it on these. |
| 2026-09-06 | #35a | **`index.html` served with no `Cache-Control`**: nginx sends the kiosk's `index.html` with only `Last-Modified`/`ETag`, so a browser may keep the previous build's page and bundle across a plain reload (Chrome did, 2026-09-06, until `?nocache=1`). `main.jsx`'s stale-chunk reload catches the case where an old page asks for a chunk that no longer exists, not the case where the whole old bundle still exists. Fix: `add_header Cache-Control "no-cache"` for `index.html` in the nginx site, hashed assets long-lived. |
| 2026-09-06 | Operator design | **Hydrants along the route of travel**: the operator wants the closest hydrant or two *along the approach* to a call, then a third by ring or straight-line. The workstation already does most of it in `MapBoard.jsx` (`nearestHydrants`): hydrants within 300 m of the target, distance measured to the parcel's Alpha (address-side) frontage segment facing the route end, kept only if within 25 m of the OSRM route line, top 3 — none of those three constants is sourced (CLAUDE.md 6.3), and the kiosk view has none of it (the dispatch map's layer measures straight-line only, #74). To build: expose the route geometry the kiosk's `RoutingOverlay` already draws, order route-side hydrants by distance-before-arrival along the route (last hydrant passed first), then the nearest straight-line one, labelled which is which. **Ask the operator first** (7.6): how far back along the approach still counts (300 m? the last block?), whether the far side of the road counts (a supply line across traffic), whether private hydrants count, and what the department's practice is (forward lay from the last hydrant vs. reverse lay) so the ordering matches how crews actually use it. |

| 2026-09-06 | Operator design | ✅ **BUILT 2026-09-09** (`ux_notes.md` §3 item 7; `map/HallRoutesOverlay.jsx`, `utils/hallRoutes.js`, tests in `frontend/tests/hallRoutes.test.mjs`). **Every responding hall's route on the same map**: when units from more than one hall respond, draw each hall's recommended route in that hall's colour at reduced opacity beside the home hall's, so a driver sees where everyone else is coming from. OSRM already routes per unit for the ETAs (`calculateEVORouteMetrics`), so the origins exist; needs a hall colour table (`STATIONS`), one OSRM call per distinct hall, and a rule for which route is drawn on top. Kiosk shows its own hall's route solid, others translucent. |
| 2026-09-06 | #35a | **The API test suite writes to the kiosk's live database**: `backend/tests/test_api_routers.py` saves a Street View override for `5000 TESTING WAY`, and with `DATABASE_URL` pointing at the kiosk (the only Postgres there is) the row lands in production `parcels` (found 2026-09-06, updated that day by a local test run). Tests should run against a scratch schema or clean up after themselves; until then, know the row is a test artefact. |
| 2026-09-08 | Operator design | **Street labels are drawn twice above z14, and cannot be fixed with these tiles.** Cadastral's tile set begins at z14 (`/services/cadastral`: `"minzoom": 14`) and carries its own road names and addresses, while the basemap keeps its labels: the dispatch map is doubled from z14 up, the console from z14 to z15 (its rule fires at z16). Fixed and reverted the same day (`52ff82f`, reverted): labels here are not an overlay but two separate tile services, `street` and `street_nolabels`, so turning them off is a full basemap change and Leaflet rebuilds the tile layer mid-zoom. Operator on seeing it: *"it looks like from 13 to 14 we are changing map baselayers"* -- a basemap reload on the crew-facing map is worse than the doubling. The console had swapped at z16 for as long as the rule existed; the revert restores that and removes the swap from the dispatch map, which never had one. **Route out: vector basemap styling**, where labels are a style property that can be turned off without touching the tiles underneath. The operator intends to look at that; until then doubled labels above z14 is the accepted state. A labels-only raster overlay (crawled and compiled alongside the others) would also work and is the cheaper option if vector tiles stall. **Answered 2026-09-09**: the vector basemap is live; while the cadastral overlay is on, its road names stop at zoom 14 as a zoom range on three style layers, no tile reload (`docs/briefings/vector_basemap_trial_2026-09-09.md`). |
| 2026-09-09 | Operator design | **Tilt and 3D buildings on the vector basemap** (operator: "future thinking"). MapLibre can pitch the map and extrude the `building` layer, and the tiles already carry `render_height`. Two prerequisites before it is worth anything: (1) **heights from the City's LiDAR building footprints, not OSM tags** -- OpenMapTiles takes height from OSM `height`/`building:levels` and defaults the rest, so an untagged tower and a house extrude the same, a fabricated value crews cannot tell from a real one (CLAUDE.md 6.1; the measurement first is the share of `building` features in the extract with a real height); (2) **the map surface must be MapLibre-only** -- Leaflet cannot pitch, so every overlay (zones, hydrants, parcels, routes, closures) is rewritten, the larger project the vector-basemap brief names. Also unanswered: which call type a tilted view helps a driver with that the Street View tile and the parcel outline do not. Not built; ask the operator that question before starting. |
| 2026-09-08 | Operator design | **Admin-gate the two saves**: the arrival point (`POST /api/parcels/entrance`) and the Street View view (`POST /api/parcels/streetview`) are operator rulings on a shared screen; the operator does not want them crew-facing. Reuse the review screen's admin login as an *unlock* in the workstation header, render the save controls only while unlocked, require the JWT on both endpoints. Reading stays open. ~1 h. |
| 2026-09-08 | Operator design | **Admin-gate the two saves**: the arrival point (`POST /api/parcels/entrance`) and the Street View view (`POST /api/parcels/streetview`) are operator rulings on a shared screen; the operator does not want them crew-facing. **Built 2026-09-09** (`ux_notes.md` §3): the header padlock, the session hook, `require_admin` on the four save routes; the client's silent self-login with the password in the source is gone, and the token signing key comes from the root `.env` like the password (the old default was a literal in git history). **Still open**: an auto-lock for production, the operator's call on the interval. |
| 2026-09-09 | Routing stream, closing #1 | **The routing speed table is OSRM's stock `car.lua` figures** (motorway 90 … residential 25, service 15 km/h; `docs/standards/osrm/car.lua` lines 169–188), unmeasured for this city or these vehicles, and every ETA a crew reads comes from them. The apparatus profile deployed 2026-09-09 changed rules only, by ruling. Source, in order: **CAD unit-status times** (en route, on scene) per unit joined to the OSRM route for that call, which calibrates hills, intersections and traffic at once and is the falsifier for any speed model; manufacturer spec sheets for the actual apparatus (gradeability, governed top speed) as a ceiling; NFPA 1901 performance minimums (not held) as a floor. Hill speeds are mechanically available in the graph (`process_segment` with a raster elevation source, the image ships `rasterbot.lua` as the example; the City's LiDAR DEM is the input) once the numbers have a source. One graph per apparatus class only if the measured difference between classes exceeds the ETA's own error. Operator ruled 2026-09-09: one rule set for every apparatus, emergency response assumed. Do not touch the table by hand (CLAUDE.md §6.3, §7.2). |
| 2026-09-09 | Operator request | **Mobile accessibility review**: the frontend has no responsive layout in practice (18 breakpoint uses, none in the surfaces), fixed-width chrome, fit padding written as literals that exceed a phone-width map, two crew-facing facts (flag reasons, changed fields) shown only on hover, and 14 of 18 controls under 44 px on every screen including the kiosk touchscreen. Reviewed and planned in [`briefings/mobile_accessibility_review.md`](briefings/mobile_accessibility_review.md); the operator promoted the phone surface the same day and Phases 1 to 3 were built (its §7); Phase 4, the public endpoint, is not. |
| 2026-09-09 | Operator design, 3A | ✅ **BUILT 2026-09-09** on `claude/mobile-accessibility-review-v8cih3`, unconfirmed on the kiosk. Artboard 3A of the operator's Claude Design canvas (`docs/design/`) is the dispatch display: three header cards, the route pill, the control stack, the hydrant card, two view tiles with header bars; the details box is gone (#74). Departures recorded in `docs/briefings/mobile_accessibility_review.md` §7. **Left from the canvas:** (a) **3C, the expand state** (EXPAND opens a tile over the body with the header untouched and the route map shrinking to a tile); (b) the **ROAD CLOSURE** notice needs the route-corridor closure check (`road-closure-management` skill, a specification) and the **OCCUPANCY** notice needs an occupancy record that does not exist, so neither is built (CLAUDE.md §6.1); (c) the canvas's **Archivo and JetBrains Mono** faces are Google Fonts, a WAN fetch; vendoring the two (both OFL) into the bundle would give the kiosk the canvas's type without an external call; (d) ~~one unit for hydrant distances~~ **ruled the same day: feet everywhere** (*"we use ft for hose lay lengths"*), built in the card, the map tooltips and the console. Also ruled: the TV-mode toggle is removed for good; the units card leads with the first-due unit by OSRM ETA. |
| 2026-09-10 | Operator design | **Documents on the dispatch display, beyond the pre-incident plan.** The hydrant card's foot is a documents slot (operator, 2026-09-10: "a placeholder note, or button under hydrants ... for bringing up a stored pre-incident plan document, or other special documents"). Built: the pre-incident plan, when `target.pre_plan_pdf_url` is set, opening `PrePlanModal`; no dispatch in the corpus carries one, so what renders today is *NO STORED DOCUMENTS*. **Open, and the operator's to answer**: what the other document kinds are (hazmat sheets, site plans, gate codes, standing orders?), where they are stored and how one is attached to an address, whether they are per-parcel like the arrival point and the Street View or per-incident-type, and who may add one. Until that is answered nothing else can be built here without inventing a domain model (CLAUDE.md §7.2). |
| 2026-09-10 | #77 (**FIXED `ec34d27`**, crew-visible) | **An arrival point is written to one row and read from another, so it never takes effect on a multi-unit site.** `public.parcels` is one row per ADDRESS: 71,212 rows over 27,855 gis_ids, 43,842 rows (62 %) sharing a gis_id with a different address, 2,170 duplicated addresses, and one Coquitlam Centre gis_id covering 235 suites while 1,671 rows carry no gis_id at all, and the base row for that very address is one of them (measured on the kiosk 2026-09-11; an earlier note here said 1,671 suites under one gis_id, which was wrong). **The save** now writes the row the client names (`parcel_id`, fixed `d4989cd`), which for a searched or dispatched address is the base row, e.g. `2929 Barnet Hwy`. **The resolver** does not choose that row: `address_resolver.py` selects `WHERE house = :house`, scores the street, and takes `scored[0]` — the first of 236 tied rows under `ORDER BY street, streettype, id`, which for 2929 Barnet Hwy is the suite row `2929 Barnet Hwy 2112`. So the operator's ruling is stored where nothing reads it. **Not a live routing error today**: all 236 rows share one `front_lat`/`front_lng`, so the destination is the same whichever row wins; only the per-row `entrance_lat` differs, which is exactly the feature that fails. Full brief: [`briefings/arrival_point_row_identity.md`](briefings/arrival_point_row_identity.md). **Ruled 2026-09-11** — the base point propagates to every suite, a suite set by hand is left alone, and the parser pulls the main row — so what is open is the key, not the behaviour: the cascade he proposed — a base building arrival point, overridden by a sub-address one — is the right model but needs the key decided. Of 617 dispatches, 289 carry a `subaddress` in 151 distinct values; 138 contain a number (`Number 201`, `Number 189 Parkwood Manor`) and 151 are pure names (`Rain City Housing`, `Coquitlam Center Mall`, `Number Basement`), and **his own example, London Drugs, is a name, not a unit**. `public.parcels.unit` holds the suite number, so a unit-keyed cascade is possible; a name-keyed one has no home (`public.custom_places`, named in CLAUDE.md §1, does not exist in the database).  **Resolved 2026-09-10:** those 1,671 no-gis_id rows *are* the `base_site` rows — CFR's master row per multi-parcel property, built since 2026-08-31 and unique per address. Nothing read them: `ParcelModel` never declared the column. `ec34d27` makes `base_site` the row every address lookup and the resolver return. Operator ruled suites out of scope; the unit cascade and the named-tenant layer (48 names, 152 dispatches, `public.custom_places` does not exist) stay here as backlog. |
| 2026-09-11 | #routing-metrics fix | **83 dispatch records hold `routing_metrics` only at the top level, not in `target`.** All fall in one window, 2026-08-14 to 08-24; every record outside it agrees across the two copies (458 of 541 routable calls). Harmless today because both readers take the column first (`api/routers/dispatches.py` `serialize_call`, and `KioskView` since `edad22d`), so nothing renders wrong — it is a shape inconsistency, not a wrong value. Found while verifying the `79a05f5` backfill; not caused by it, and the 19 records it repaired were written to both copies. |
| 2026-09-11 | #routing-metrics fix | **A phase 2 correction overrides the coordinates but not the street-section geometry derived from them.** On the CORRECTION_AUDIT path `target_payload` spreads `**p1_target` and overrides `address`, `lat`, `lng`, `rings` — but not `location_type`, `segment` or `endpoints`, which phase 1's geocode produced for the address phase 2 just replaced. `segment` is drawn on the dispatch map (`RouteOverviewPanel.jsx:435`, `DispatchTargetLayer.jsx:47`), so in principle a corrected call could highlight a stretch of the wrong street. **Latent, not occurring**: 0 of 624 records carry `endpoints`, `segment` or `location_type` at all, so either the street-section resolution never fires on real dispatches or they are still being dropped somewhere — `useMqttListener.js:126` records them being lost once before. Not investigated (freeze). The routing itself is correct: `refresh_routing_metrics` is passed `res.get("endpoints")`, from the same geocode as the coordinates. |
| 2026-09-11 | #routing-metrics fix | **`test_parcels_and_streetview_router` fails on `main`** — asserts a parcel search returns at least one result, gets 0 (`tests/test_api_routers.py:239`). Pre-existing and not from the routing work: it fails identically with those changes stashed. Sits where Explore's search was just changed (`ce6de64`, `f24e941` — one row per property rather than every suite), so it may be that or local database state; not diagnosed. The other failure in that module, `test_admin_gate_on_operator_saves`, is only the missing local `httpx2` (punch-list #10, expected on a dev laptop). |
| 2026-09-11 | #79 talk group | **`DISP-2026-07CC85` parsed its address as `4453 Port Man Bridge`.** A bridge is not a civic address and the `4453` is unexplained — the announcement is `4453 port mann bridge near trans canada highway`. Found while fixing the talk group on that call; the location side was not investigated (freeze). |
| 2026-09-11 | #79 talk group | **The fallback talk-group regex ends at `map grid` or end of text** (`parser/announcement.py:283`), so a transcript with no map grid hands the whole remainder to `match_radio_channel`, map-grid digits included. Contained by the digit stage now returning `None` on two channel digits rather than taking the earlier one, not correct — the bound should be the announcement structure, not a keyword that may be absent. |
| 2026-09-11 | #79 talk group | **Why does Whisper drop the "10" from "talk group 10 combined response coquitlam"?** Measured, not explained: **39 of 434** channel-10 calls lose the digit in every round, and **11 of those** have an operator-verified transcript that reads `use talk group 10 combined response coquitlam` — so it is spoken. Two further calls (`DISP-2026-E365BF`, `DISP-2026-78C19A`) keep it in one round and lose it in the other, same broadcast. `parser/sanitize.py` was checked and never touches digits after "talk group", so it is the STT, not us. The acoustic or model reason is **not established** and is deliberately not guessed at (§7.7). It matters because the digit is channel 10's only word of its own once the venue channels carry "Combined Response" too. |
| 2026-09-11 | #79 talk group | **`public.vocabulary` holds the channels that matter, not all of them** (operator, 2026-09-11 — left incomplete deliberately during the freeze). A channel that is absent mostly resolves to `None`, which is correct — `11 coquitlam`, `combined venue golden ears`, `tac 1` all do. But an absent channel sharing a listed one's distinguishing word is captured by it: `combined venue port moody` and `combined venue port coquitlam` both return **Combined Venue Port Mann**, because `port` belongs to no other listed channel. Measured 2026-09-11; **not a regression** — the pre-#79 rule returns the same, verified against `HEAD~1`. Port Moody and Port Coquitlam are adjacent municipalities, so this is only hypothetical for as long as CFR has no combined-venue channel with either. Completing the list closes it; nothing in code can. |

